export default class IncidentActivity {
    constructor(current_activity, most_recent_action) {
        this.current_activity = current_activity;
        this.most_recent_action = most_recent_action;
    }
}